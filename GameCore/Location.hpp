#pragma once

namespace Candies
{
    namespace GameCore
    {
        struct Location
        {
            unsigned x, y;
            Location(unsigned x, unsigned y) : x(x), y(y) { }
        };
    }
}
